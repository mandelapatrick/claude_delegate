{
  "PostInstall": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "command",
          "command": "cd ${CLAUDE_PLUGIN_ROOT}/mcp-server && npm install && npm run build"
        }
      ]
    }
  ]
}
